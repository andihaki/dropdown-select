const Wrapper = "flex gap-1";
const Empty = "my-1 py-0.5";
const Selected = `${Empty} my-1 py-0.5 px-4 bg-gray-100 rounded-full flex gap-1 cursor-default text-nowrap`;

const cx = {
  Wrapper,
  Empty,
  Selected,
};

export default cx;
